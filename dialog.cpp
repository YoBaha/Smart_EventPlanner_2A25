#include "dialog.h"
#include "ui_dialog.h"
/*
Dialog::Dialog(QWidget *parent) :
    QDialog(parent),
    ui(new Ui::Dialog)
{
    ui->setupUi(this);
}

Dialog::~Dialog()
{
    delete ui;
}
void Dialog::setaj(Ajout a)
{
 ui->lineEdit_nom->setText(a.get_nom());
 ui->lineEdit_ads->setText(a.get_ads());
 ui->lineEdit_ofr->setText(a.get_ofr());
 ui->lineEdit_num->setText((a.get_num()));
 ui->lineEdit_mail->setText(a.get_mail());
}
*/
